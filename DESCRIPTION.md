# Instatic

Instatic is an open-source visual CMS that publishes plain HTML and CSS. Authors manage content and design through its protected `/admin` interface.

This Cloudron package provides PostgreSQL, persistent uploads, backups, health monitoring, proxy-aware origins, and a per-install encryption key. Instatic `0.0.19` stores native form submissions but does not send submission emails.
