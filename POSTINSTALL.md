## Instatic setup

Open **$CLOUDRON-APP-ORIGIN/admin** and create the owner account. No default credentials are generated.

Instatic manages its own accounts; Cloudron users are not automatically signed in. CMS-native forms save submissions to their configured Instatic data tables. Upstream Instatic `0.0.18` does not send form-submission email notifications.
